export type SvgType = {
  size?: string;
  color?: string;
};

export type BgSphere = {
  size: string;
  top: string;
  left?: string;
  right?: string;
  color: "#5c45fd" | " #a89bff" | " #161616" | "#969799" | "#f5f5f5";
};
